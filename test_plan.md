# Triply Telegram Manual Test Checklist

1. `Привет, хочу в Турцию` -> бот не запускает анкету, спрашивает следующий недостающий слот (обычно ночи), затем бюджет.
2. `отмена` / `/cancel` / `стоп` в любом состоянии -> мгновенный сброс, сообщение про остановку поиска, кнопки 6 стран + `🔎 Новый поиск`.
3. `Хочу во Вьетнам` -> честный ответ про 6 поддерживаемых стран, без поиска и без подмены страны.
4. `Покажи туры на Мальдивы в сентябре в районе 100000` -> бюджет не спрашивается повторно; спрашиваются только реально отсутствующие поля (например, ночи).
5. После выдачи: `Есть в сентябре?` -> refinement последнего поиска, не новый guided flow.
6. После выдачи Турции: `Хочу на Сейшелы на 10 ночей` -> страна реально меняется на Сейшелы.
7. После выдачи: `за 250к?` -> бот уточняет, это максимум или ориентир, а не молча ставит `budget_max=250000`.
8. Двойной тап `💚 Хочу этот тур` -> нет дублей, бот отвечает `Уже проверяю этот тур ✅`.
9. Inline country/nights/budget кнопки компактные (2x3 для стран, 2x2 для nights/budget) и callbacks срабатывают.
10. `Что ты умеешь?` / `привет` в любом состоянии -> meta/smalltalk ответ, без продолжения текущей анкеты.
11. `/start` -> мгновенный ответ с приветствием и кнопкой `🔎 Найти тур`; в логах при `LLM_DEBUG=1` видно `start handled`.
12. `/cancel` в любом состоянии -> такой же reset, как текстовая `отмена`.
